fastapi==0.115.0
uvicorn[standard]==0.30.6
strawberry-graphql[fastapi]==0.239.2

sqlalchemy[asyncio]==2.0.35
asyncpg==0.29.0
alembic==1.13.2

pydantic==2.9.2
pydantic-settings==2.5.2

# passlib 1.7.4 (last release, 2020) has a broken bcrypt-version probe
# against bcrypt>=4.1 (which removed the `__about__` attribute passlib's
# backend loader reads) -- surfaces as a bogus "password cannot be
# longer than 72 bytes" ValueError on the very first hash() call, since
# passlib's own internal self-test fails before your actual password is
# ever touched. Pinning bcrypt to the last 4.0.x release keeps the
# `__about__` attribute passlib expects.
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-jose[cryptography]==3.3.0

celery==5.4.0
redis==5.0.8

# Notifications: SMS + Email for high/urgent priority tickets
twilio==9.3.2
# Email uses stdlib smtplib — no extra package required. Swap
# app/services/notifications.py's _send_email for the sendgrid/boto3 (SES)
# SDK if you'd rather not run/manage SMTP credentials directly.

pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2
